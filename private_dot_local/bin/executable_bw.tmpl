{{- if eq .chezmoi.os "linux" -}}
{{- if .integration.bitwarden.enabled -}}
#!/bin/bash

# Run Bitwarden CLI with no deprecation warnings
NODE_OPTIONS="--no-deprecation" exec $HOME/.local/wrapped/bw "$@"
{{ end -}}
{{ end -}}
